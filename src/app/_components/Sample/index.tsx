"use client";

import { Button } from "react-aria-components";
// import { button as styles } from "./styles.css";
// import { button as styles } from "@/app/_components/Sample/styles.css";
// console.log(styles);

const Sample = () => {
  return (
    // <Button onPress={() => alert("Hello world!")} className={styles.main}>
    <Button onPress={() => alert("Hello world!")}>Sample</Button>
  );
};

export default Sample;
